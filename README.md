# stock-intel-agent

Vite + React + Tailwind starter with a Stock Intel dashboard UI scaffold and lightweight shadcn-style UI components.

## Quick start

```bash
npm install
npm run dev
```

## Notes

- Keep `.env` local only.
- Commit `.env.example` to share required env keys.
- `@` alias maps to `src/`.